@model WebApplication1.Models.Experience

@{
    ViewBag.Title = "Delete Experience";
}

<h2>Delete Experience</h2>

<h3>Are you sure you want to delete this experience?</h3>

<div>
    <h4>Experience</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Title)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Description)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Description)
        </dd>
    </dl>

    @using (Html.BeginForm("DeleteExperienceConfirmed", "People", FormMethod.Post))
    {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(model => model.ExperienceID) <!-- Ensure this matches your parameter in the controller -->
        <button type="submit" class="btn btn-danger">Delete</button>
        <a href="@Url.Action("Index", "People")" class="btn btn-secondary">Cancel</a>
    }
</div>
